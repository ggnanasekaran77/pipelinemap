app:
  projectName: demo
  name: configserver
  build: 
    type: gradle
    command: clean build artifactoryPublish
    wrapper: True
  strategy:
    demo: disabled
  git:
    url: https://github.com/ggnanasekaran77/configserver.git
ansible:
  default:
    first:
    - playbookPath: tomcat
      playbookName: deployment.yml
      extraVars: -i inventories -e targetEnv=all 
    confirmation: False
    second:
    - playbookPath: tomcat
      playbookName: deployment.yml
      extraVars: -i inventories -e targetEnv=all
    confirmation: False
notification:
  teams:
    default:
    prd: 
  
